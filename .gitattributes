# Normalise to LF in the repository regardless of the checkout platform,
# so Windows and CI never disagree about what changed.
* text=auto eol=lf

# PowerShell is happiest with CRLF on disk.
*.ps1 text eol=crlf

# Never touch these.
*.png binary
*.jpg binary
*.pdf binary
*.xls binary
*.xlsx binary
